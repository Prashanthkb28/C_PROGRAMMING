/* Documentation
Name: PRASHANTH KB
Date: 24/08/2023
Description
sample Input:
Sample Output:
*/
#include<stdio.h>
int main()
{
   //  printf("C program %s","class by");    c program class by
  // printf("C program %s","class by %s me","wow");  warning but o/p-c program class by %s me
   
   
    /* int proskills=4;
    printf(3+"proskills"+4);   o/p--> ls ( it will add numbers specified and tremoves that much chars and print remaning char) */
   
   
    /*    
      int a,n1,n2;
    printf("enter 2 number:");
    a=scanf("%d %d",&n1,&n2);  // the sucessful of reading the scanf returns 1 else 0
    printf("a=%d\n",a); 
    // o/p===> a=2 ( first its prompt for scanf and returns number of sucess  and returns  to variable a ,if its sanf for 1 var then returns 1) if scanf 1 2 then a=2 if scanf for 1 then a=1 if scanf() for 1 r for int then a=1*/

    /*int a;
    a=printf("hello world");
    printf("%d",a);    o/p---> first print and then a=11 it returns the no of char    if %c is used then it prints the char according to ascii table if no of cchar above 32 
   */
        return 0;

}
