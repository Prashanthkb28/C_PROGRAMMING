#include<stdio.h>
int main()
{
    int y;
    y=10/4*2+3-5%2;
    printf("%d is the output of the result with int\n",y);
    float res;
    res=10/4*2+3-5%2;
    printf("%f is the output of the result with float\n",res);
    return 0;
}
